version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATABASE_HOST=localhost
      - DATABASE_PORT=5433
      - DATABASE_USERNAME=postgres
      - DATABASE_PASSWORD=07101960qwe
      - DATABASE_NAME=postgres
    depends_on:
      - db

  db:
    image: postgres:13
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 07101960qwe
      POSTGRES_DB: localhost
    ports:
      - "5432:5432"